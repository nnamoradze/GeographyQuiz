<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basketquiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_geography"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_geography_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasketQuiz">
        <activity
            android:name="com.example.basketquiz.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>